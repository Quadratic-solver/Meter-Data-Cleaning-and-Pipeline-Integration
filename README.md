# Meter-Data-Cleaning-and-Pipeline-Integration
The project reads messy raw data from CSV files and extracts serial numbers, FL, and LL values. It cleans and resolves duplicate extracted records and resolve data conflicts and unresolved rows for manual review
